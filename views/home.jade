
h1 Submit the form to make a call!


form(method="post", action= serveropts.formActionUrl )
  fieldset
    legend General
    p
      label(for="user[name]") Username:
        input(type="text", name="user[name]", value=user.name)
    p
      label(for="user[phone]") Phone Number:
        input(type="text", name="user[phone]", value=user.phone)
      |  Enter a valid 
      | US Phone Number with a the country code (+1) and no other characters but numbers
      | i.e. <em> +16269239971 </em>.
    p
      label(for="user[email]") Email:
        input(type="text", name="user[email]", value=user.email)
        .tip
          | Enter a valid 
          | email address 
          | such as <em> youremail@malinator.com</em>.
  
    p.buttons
      input(type="submit", value="Save")
